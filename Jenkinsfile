	pipeline {
		agent any
		    stages {
		        stage('run') {
		            steps {
		                echo 'Running Java Code...'
		                sh 'javac Hello.java'
		                sh 'java Hello'
		            }
		        }
		    }
		}
